{
  "name": "typhoon-bavi-tracker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "巴威台风实时路径追踪与可视化系统 · Cloudflare Worker + MapLibre GL",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "maplibre-gl": "^5.6.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260620.0",
    "@types/qrcode": "^1.5.6",
    "typescript": "^5.8.3",
    "vite": "^7.0.0",
    "wrangler": "^4.86.0"
  }
}
